## Example of `out`

### Reading a configuration file

**Method Call**

```csharp
ReadConfiguration(out string serverName,
                  out int port,
                  out int timeout);
```

**Output**

```text
serverName = localhost
port       = 8080
timeout    = 30
```