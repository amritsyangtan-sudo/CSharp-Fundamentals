Variable Declaration:
    variable declaration is the process of creating the variable by specifiing
        its datatype
        its name
        example: int age;
        This statement doesnot store a value.
        It creates and integer type, variable named age.
Declaration:
    int age;
Initialization:
    int age = 25;

How it works Internally?
    int age:
        compiler reads the keyword int and recognizes the data type, it reads age and it becomes the variable's idenfier
        Variable name : age
        Data type : int
    Compiles knows an int occupies 4 bytes. it reserves enough memory for that variable .
    