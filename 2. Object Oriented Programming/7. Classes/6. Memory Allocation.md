```csharp
    Customer customer = new Customer();

    Where is the Customer stored ?

The computer has one big memory
+---------------------------------------------+
|                                             |
|                  RAM                        |
|                                             |
+---------------------------------------------+

Inside the memory CLR  organizes the differne regions.

Program memory

+---------------------+
| Stack               | Stack is designed for method calls, local variables, parameters, return address
+---------------------+
| Heap                | Unlike int, the Customer object is not stored in stakc, the object goes to heap.
+---------------------+

                            HEAP
                            +----------------------+
                            | Customer Object      |
                            |----------------------|
                            | Name = null          |
                            | Email = null         |
                            +----------------------+


Customer customer = new Customer();
STACK

+----------------------+
| customer = 0x1000    |    customer contains the address where the object is.
+----------------------+

            │
            ▼

HEAP

Address 0x1000

+----------------------+
| Customer Object      |
+----------------------+

````

# Scenario
```csharp
    Customer customer1 = new Customer();
    Customer customer2 = customer1;

    suppose customer1 address i 0x1000
    customer2 gets the address that is 0x1000

    now both variable point to the same object.
```